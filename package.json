{
  "name": "inter-guild-buildathon-sdr",
  "version": "0.1.0",
  "description": "Autonomous SDR system backend (Phase 1: schema + CRUD)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "seed": "node db/seed.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  }
}
